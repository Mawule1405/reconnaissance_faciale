class Image:
    def __init__(self, image_id, session_id, image_path, timestamp):
        self.image_id = image_id
        self.session_id = session_id
        self.image_path = image_path
        self.timestamp = timestamp
